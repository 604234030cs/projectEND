import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { CheckDistancePage } from './check-distance';

@NgModule({
  declarations: [
    CheckDistancePage,
  ],
  imports: [
    IonicPageModule.forChild(CheckDistancePage),
  ],
})
export class CheckDistancePageModule {}
